import { bootstrapApplication } from '@angular/platform-browser';
import { MainComponent } from './app/components/main/main.component';


const bootstrap = () => bootstrapApplication(MainComponent);

export default bootstrap;
